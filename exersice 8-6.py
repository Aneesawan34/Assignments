def city_country(city, country):
    list = [city, country]
    # a = city +", " + country
    return list
stored = city_country('karachi', 'pakistan')
print(stored)
stored = city_country('Lahore', ' pakistan')
print(stored)
stored = city_country('Islamabad', 'pakistan')
print(stored)